<Page
    x:Class="PRTGInsight.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PRTGInsight.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Settings"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="20">
                <!-- Connection Settings -->
                <StackPanel>
                    <TextBlock Text="Connection Settings" 
                               Style="{StaticResource SubtitleTextBlockStyle}" 
                               Margin="0,0,0,12"/>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel Spacing="12">
                            <TextBlock x:Name="ServerUrlTextBlock" 
                                       Text="Server URL: --" 
                                       TextWrapping="Wrap"/>

                            <TextBlock x:Name="AuthTypeTextBlock" 
                                       Text="Authentication: --" 
                                       TextWrapping="Wrap"/>

                            <TextBlock x:Name="VersionTextBlock" 
                                       Text="PRTG Version: --" 
                                       TextWrapping="Wrap"/>

                            <Button x:Name="ReconnectButton" 
                                    Content="Reconnect" 
                                    Style="{StaticResource AccentButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- App Settings -->
                <StackPanel>
                    <TextBlock Text="App Settings" 
                               Style="{StaticResource SubtitleTextBlockStyle}" 
                               Margin="0,0,0,12"/>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel Spacing="12">
                            <ToggleSwitch x:Name="ThemeToggle" 
                                          Header="Theme" 
                                          OnContent="Dark" 
                                          OffContent="Light"/>

                            <ToggleSwitch x:Name="AutoRefreshToggle" 
                                          Header="Auto Refresh" 
                                          OnContent="On" 
                                          OffContent="Off"/>

                            <NumberBox x:Name="RefreshIntervalBox" 
                                       Header="Refresh Interval (seconds)"
                                       Minimum="30"
                                       Maximum="3600"
                                       Value="300"
                                       SpinButtonPlacementMode="Compact"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- About -->
                <StackPanel>
                    <TextBlock Text="About" 
               Style="{StaticResource SubtitleTextBlockStyle}" 
               Margin="0,0,0,12"/>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel Spacing="12">
                            <TextBlock Text="PRTG Insight" 
                       Style="{StaticResource SubtitleTextBlockStyle}"/>

                            <TextBlock Text="Version 6 maart 2025" 
                       Style="{StaticResource BodyTextBlockStyle}"/>

                            <TextBlock Text="© 2025 Danny Janmaat" 
                       Style="{StaticResource BodyTextBlockStyle}"/>

                            <HyperlinkButton Content="GitHub" 
                             NavigateUri="https://github.com/DannyJanmaat"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
